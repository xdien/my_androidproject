<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crazy.xdien.suaanh" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".filebrowser.FileChooser"
            android:label="@string/hello_world">
        </activity>
        <activity android:name=".img_effects.Smoonthing_Ext"
            android:label="@string/hello_world">
        </activity>
        <activity android:name=".img_effects.Smoothing"
            android:theme="@android:style/Theme.Dialog"
            android:label="@string/please_wait">
        </activity>
        <activity android:name=".img_effects.Threshold"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>
